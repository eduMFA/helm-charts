To make a new release, **update the changelog** and then push a new tag with the
format: `edumfa-x.y.z`.  
Make sure the version is equal to the one in Chart.yaml!

Example:  
```sh
$ git tag edumfa-0.3.15
$ git push origin tag edumfa-0.3.15
[...]
 * [new tag]         edumfa-0.3.15 -> edumfa-0.3.15
```
